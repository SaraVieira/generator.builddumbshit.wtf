import React from "react";

export default () => (
  <>
    <h1>API</h1>
    <h3>There are two endpoints you can use to get the ideas showed here:</h3>
    <a href="/api/all">/api/all</a> will return all the ideas
    <br />
    <a href="/api/random">/api/random</a> will return all the ideas
    <br />
  </>
);
